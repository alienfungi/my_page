.hidden-xs
  %nav.navbar
    %ul.nav.nav-pills.nav-stacked
      - @sidebar_links.each do |text, path|
        = content_tag :li, class: (current_page?(path) ? 'active' : '') do
          = link_to text, path

