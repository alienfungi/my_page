- provide :title, 'New User'

= form_for @user, html: { role: "form" } do |f|
  .form-group
    = f.label :email, "Email", class: "control-label"
    = f.text_field :email, class: "form-control", autofocus: true

  .form-group
    = f.label :username, "Username", class: "control-label"
    = f.text_field :username, class: "form-control"

  .form-group
    = f.label :new_password, "Password", class: "control-label"
    = f.password_field :new_password, class: "form-control"

  .form-group
    = f.label :new_password_confirmation, "Confirm Password", class: "control-label"
    = f.password_field :new_password_confirmation, class: "form-control"

  = f.submit "Submit", class: "btn btn-default"

