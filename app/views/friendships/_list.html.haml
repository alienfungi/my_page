- if @friends.empty?
  You don't have any friends :(
- else
  %table.table
    %tbody
      - @friends.each do |friend|
        %tr
          %td= link_to friend.username, friend
          %td
            .pull-right
              = link_to new_message_path(recipient: friend.username), class: "btn btn-sm btn-primary" do
                %span.glyphicon.glyphicon-envelope
                Message
